# 작업 02

Main/Rod 재현 빌드와 VS2022 환경

## 목표

Arduino IDE를 열지 않고 Main과 Rod를 각각 또는 함께 빌드할 수 있는 명령과 VS2022 실행 경로를 구축한다.

## 현재 기준

- 기존 upload batch는 있으나 source compile script는 없다.
- 프로젝트 로컬 Arduino CLI 1.5.2-rc.1, ESP32 Core 2.0.17, ESP32Servo 1.1.2가 설치되어 있다.
- Main/Rod 공통 전체 FQBN은 `PROJECT_COMMANDS.md`에 확정되어 있다.
- Visual Studio Professional 2022 17.14가 설치되어 있다.

## 관련 영역

- 향후 `tools/`, `docs/build/`, VS2022 solution 또는 Open Folder 설정

## 구현 원칙

- MSVC가 펌웨어를 직접 컴파일하지 않는다.
- Arduino CLI가 실제 compiler driver다.
- COM 포트 등 PC별 값은 Git에서 제외한 로컬 설정으로 둔다.

## 세부 작업

### A. Arduino CLI 환경 구성 문서 (완료: 2026-08-27)

- 확정 버전, Core URL, Core 2.0.17 및 외부 라이브러리 준비 절차를 기록한다.

### B. Main build script (완료: 2026-08-27)

- 입력, 출력 경로, FQBN options 및 실패 code를 명시한다.

### C. Rod build script (완료: 2026-08-27)

- Main과 독립적인 sketch와 artifact 경로를 사용한다.

### D. 통합 build 및 artifact 확인 script (완료: 2026-08-27)

- Main/Rod를 순차 실행하고 결과 파일과 크기를 요약한다.

### E. VS2022 연결 (완료: 2026-08-27)

- Open Folder 또는 NMake 방식으로 build 명령을 연결하고 IntelliSense include 경로를 문서화한다.

### F. 재현 빌드 게이트 (완료: 2026-08-27)

- Codex가 VS2022 MSBuild로 clean Main/Rod build를 직접 수행하고 산출물을 확인한다.

### G. VS2022 IntelliSense Arduino 참조 복구 (완료: 2026-08-27)

- Main/Rod가 공통 ESP32-S3 Core/SDK/library include와 전처리 정의를 사용하도록 구성한다.
- Arduino builder가 자동 삽입하는 `Arduino.h`를 VS IntelliSense forced include로 재현한다.

### H. 버전별 배포 산출물 관리 (완료: 2026-08-27)

- 중간 산출물은 `artifacts/`에 유지하고 플래시용 파일만 `bin/<configuration>/<platform>/<version>/`에 배포한다.
- Main/Rod 버전 폴더마다 application, bootloader, partitions, `boot_app0.bin`의 4개 파일만 둔다.
- 현재 배포 폴더 버전은 각 활성 소스 `Version.cpp`를 단일 기준으로 삼고 `tools/firmware-versions.cmd`/`.ps1`이 동적으로 읽는다(2026-08-31 보완).

## 진행

잔여 작업 0개

## 변경 금지

- source logic, Core 버전, protocol 및 firmware 설정을 동시에 변경하지 않는다.

## 완료 조건

- 사용자 PC에서 명령 한 번으로 Main/Rod firmware가 생성되고 VS2022에서 같은 명령을 호출할 수 있다. (충족: 2026-08-27)

## 시험 방법

- F에서 VS2022 MSBuild `Release|x64` 전체 빌드 결과를 확인한다.

## 결과

- 2026-08-27 작업 02-A 완료: `PROJECT_COMMANDS.md`와 `docs/build/ARDUINO_REFERENCE_AUDIT.md`에 프로젝트 로컬 설치 경로, Core/library 설치 절차, 전체 FQBN 및 복구 근거를 기록했다.
- 2026-08-27 순서 조정: 01-F와 02-F를 같은 build 게이트로 합치고, 실제 VS2022 solution 준비 후 최초 clean build를 수행한다.
- 2026-08-27 작업 02-B 완료: `tools/arduino-env.cmd`와 `tools/build-main.cmd`를 작성했다. Main clean build는 원본 build 폴더를 덮어쓰지 않고 `artifacts/build/DF_Main`과 `artifacts/firmware/DF_Main`을 사용하며 필수 flash 파일 4개를 검사한다.
- 2026-08-27 작업 02-C 완료: `tools/build-rod.cmd`를 작성했다. Rod는 Main과 독립된 `artifacts/build/DF_Rod`, `artifacts/firmware/DF_Rod`를 사용하며 필수 flash 파일 4개를 검사한다. 원본 폴더명과 `DF_Rod.ino` 불일치는 `artifacts/sketch/DF_Rod` clean staging으로 해결했다.
- 2026-08-27 작업 02-D 완료: `tools/build-all.cmd`를 작성했다. Main과 Rod를 순차 clean build하고 첫 실패 code를 반환하며, 성공 시 두 application binary의 크기와 output 경로를 요약한다.
- 2026-08-27 작업 02-E 완료: `DF_Firmware.sln`과 `vs/`의 Main/Rod NMake project를 생성했다. 중복 집계 project는 제거하고 Rod가 Main에 의존하도록 구성해 Solution Build가 Main 다음 Rod를 각각 한 번씩 실행한다.
- 2026-08-27 작업 02-E 검증: 두 `.vcxproj` XML과 source item 경로, Debug/Release x64 solution configuration을 확인했다. MSBuild 속성 평가로 NMake command가 저장소 루트 `tools/`와 `artifacts/`를 가리킴을 확인했으며 compile target은 실행하지 않았다.
- 2026-08-27 Rod 경로 문제 해결: `Vr1.0.1.0` 폴더명과 `DF_Rod.ino` 이름 불일치로 Arduino CLI가 원본 폴더를 sketch로 열지 못하는 문제를 발견했다. `tools/stage-rod.cmd`가 Git 제외 `artifacts/sketch/DF_Rod`를 매번 재생성하도록 수정했다.
- 2026-08-27 Rod staging 검증: 원본과 staging의 `.ino/.cpp/.h` 15개 SHA-256이 모두 일치했고 Arduino CLI `--show-properties`가 exit code 0으로 ESP32-S3 build property를 생성했다. compiler는 실행하지 않았다.
- 2026-08-27 작업 02-F 경로 수정: Windows GCC 8.4가 긴 경로의 `no-rtti/bits/c++config.h`를 열지 못하는 문제를 확인했다. `arduino-env.cmd`가 저장소를 `X:`에 자동 매핑하고 로컬 Arduino data/user 경로를 짧게 사용하도록 수정했다. 자동 매핑과 해제를 별도 확인했다.
- 2026-08-27 작업 02-F 산출물 수정: Arduino CLI가 `boot_app0.bin`을 output directory로 복사하지 않으므로 Core 2.0.17의 고정 파일을 Main/Rod output에 명시적으로 복사하고 해시를 검사하도록 수정했다.
- 2026-08-27 작업 02-F 완료: VS2022 MSBuild `Release|x64`, `/t:Build`, `/m:1` 실행이 exit code 0으로 완료됐다. Main 882,320 bytes, Rod 763,552 bytes application과 대상별 필수 flash 파일 4개씩을 생성했다. Solution은 Main 다음 Rod를 각각 한 번씩 빌드했으며 upload/flash는 수행하지 않았다.
- 2026-08-27 직접 script 검증: 사전 `X:` 매핑 없이 CMD에서 `tools\build-rod.cmd`를 직접 실행해 자동 경로 매핑, compile/link 및 Rod 필수 산출물 4개 검사가 exit code 0으로 완료됨을 확인했다.
- 2026-08-27 작업 02-G 완료: `vs/DF_Arduino_ESP32S3.props`에 Arduino-ESP32 2.0.17의 ESP32-S3 SDK 공식 include, 실제 사용 library `FS/SPI/SPIFFS/Update/WiFi/Wire`, ESP32Servo 1.1.2, 실제 compiler 전처리 정의와 `Arduino.h` forced include를 공통화했다.
- 2026-08-27 작업 02-G 검증: Main/Rod 각각 평가된 IntelliSense 경로 207개가 모두 존재했다. `Update.h`, `FreeRTOSConfig.h`, `FreeRTOS.h`, `esp_now.h`, `ESP32Servo.h` 해석과 forced `Arduino.h` 존재를 확인했다.
- 2026-08-27 작업 02-G 회귀 검증: 공통 props import 후 VS2022 `Release|x64` Solution Build를 다시 실행해 Main 882,320 bytes, Rod 763,552 bytes와 필수 산출물 8개를 exit code 0으로 재확인했다.
- 2026-08-27 작업 02-H 완료: VS NMake 명령이 Configuration/Platform을 build/clean script에 전달하도록 수정하고, `tools/firmware-versions.cmd`의 현재 Main/Rod 버전을 사용해 `bin/release/x64/Vm1.0.9.0`과 `bin/release/x64/Vr1.0.1.0`을 생성하도록 구성했다. 각 version 폴더는 build 시 다시 만들어 stale file을 제거하며 정확히 4개 파일인지 검사한다.
- 2026-08-27 작업 02-H 검증: VS2022 MSBuild `Release|x64`, `/t:Rebuild`, `/m:1`이 exit code 0으로 완료됐다. Main 882,320 bytes, Rod 763,552 bytes를 재현했으며 두 version 폴더가 각각 4개 파일만 포함하고 8개 파일 모두 `artifacts/firmware` 원본과 SHA-256이 일치함을 확인했다. upload/flash는 수행하지 않았다.

## G 표시 오류 보완 (설정·빌드 확인: 2026-08-31)

- 사용자 첨부 화면의 `IRAM_ATTR` E0070, `vPortEnterCritical` E0020 및 후속 E0065에 대해 공통 원인 후보인 GCC `__attribute__` 해석을 보정했다. `vs/DF_IntelliSense.h`를 `Arduino.h`보다 먼저 강제 포함하고 MSVC IntelliSense 분석에서만 속성을 생략한다. 실제 펌웨어/SDK 소스와 NMake build 명령은 변경하지 않았다.
- Main/Rod Debug/Release x64 네 구성의 MSBuild 속성 평가에서 강제 포함 순서와 파일 존재를 확인했다.
- `tools/build-all.cmd Release x64` compile/link exit 0. Main application 884,857 bytes/bin 885,216 bytes, Rod application 766,713 bytes/bin 767,072 bytes, 대상별 배포 파일 4개. 로그: `artifacts/intellisense-validation/build-all.log`.
- `xtensa-esp32s3-elf-objdump -t -C`로 Main ELF의 `ISR_onTimerHandler()`가 `.iram0.text`에 유지됨을 확인했다. Main/Rod sketch compiler 의존성에 `DF_IntelliSense.h`가 없는 것을 확인했다.
- VS IDE에서 재로드/재분석 후 세 표시 오류가 사라지는지는 사용자 확인 대기다. 화면 오류 전체 해결로 기록하지 않는다. flash/장비 시험 및 작업 10-H는 수행하지 않았다.
- 잔여 구현 문자 0개(A~H 완료) 유지. 이번 G 보완의 VS 화면 확인은 남아 있다.

## 02-H 소스 버전 기반 출력 경로 보완 (2026-08-31)

- 사용자 보고: Version.cpp만 수정해 빌드하면 이전 버전 폴더로 출력됨. 소스는 Main `Vm1.0.10.0`, Rod `Vr1.0.2.0`이지만 별도 CMD 고정값은 `Vm1.0.9.0`, `Vr1.0.1.3`인 것이 원인이었다.
- `tools/firmware-versions.cmd`의 고정값을 제거하고 `firmware-versions.ps1`로 각 활성 Version.cpp의 `mainVer`/`rodVer`를 읽도록 변경했다. 기존 build/clean/통합 build와 VS NMake 호출이 같은 helper를 사용한다. 사용자는 소스 버전만 저장하면 되며 project 재로드는 필요 없다.
- 주석은 제외하고 직접 문자열 정의 하나를 읽는다. 누락·중복·조건부 정의·잘못된 버전/경로 형식은 이전 값으로 대체하지 않고 중단한다. 이전 CMD는 `deprecated/tools/firmware-versions-static.cmd.txt`에 보존했다.
- 임시 소스에서 정상 버전·공백 포함 경로·소스만 변경·주석 제외·경로 탈출·누락·중복·조건부 정의를 확인했고, 실패 시 상속된 이전 환경값이 지워지는 것까지 확인했다. 총 8개 시나리오 통과.
- `tools/build-all.cmd Release x64` compile/link exit 0. Main은 `bin/release/x64/Vm1.0.10.0`에 885,216-byte app, Rod는 `bin/release/x64/Vr1.0.2.0`에 766,848-byte app을 생성했다. 각 4파일 및 바이너리 내 버전 문자열 일치를 확인했다. 근거: `artifacts/version-output-20260831/build.log`, `result.json`, `verify.ps1`.
- firmware/libraries 87개 파일과 이전 배포 8개 파일의 작업 전후 hash가 같다. 사용자 소스 버전·로직은 수정하지 않았다. 이전 버전 폴더의 app에는 사용자가 앞서 빌드한 새 버전 문자열이 이미 포함되어 있어 폴더명만으로 과거 기준 바이너리로 판단하면 안 된다. 파일은 삭제하지 않고 보존했다.
- 이번 작업은 빌드만 수행했으며 장비 업로드는 하지 않았다. 현재 소스/빌드 버전과 마지막 장비 확인 버전은 구분한다. 02-H 보완 완료, 작업 02 잔여 0개. 전체 잔여 번호 4개(01/08/09/10), 작업 10-H 미완료 상태는 변하지 않는다.
